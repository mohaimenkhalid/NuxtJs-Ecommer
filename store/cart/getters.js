export const getCart = (state) => {
  return state.items;
}

export const getTotalPrice = (state) => {
  return state.totalPrice;
}

export const getCollapseStatus = (state) => {
  return state.collapse;
}
